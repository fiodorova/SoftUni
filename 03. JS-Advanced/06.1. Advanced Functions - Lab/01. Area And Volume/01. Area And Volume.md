**1.	Area and Volume Calculator**

Write a function that **calculates** the **area** and the **volume** of a figure, which is **defined** by its **coordinates (x, y, z)**.

**area()** </br>
function area() { </br> 
&emsp;     return Math.abs(this.x * this.y); </br> 
};

**vol()** </br>
function vol() { </br> 
&emsp;     return Math.abs(this.x * this.y * this.z); </br> 
};

**solve()** </br>
function solve(area, vol, input) { </br> 
&emsp;     //ToDo.... </br> 
}

**Input** </br>
You will receive **3** parameters -  the **functions area** and **vol** and a **string**, which contains the figures' coordinates. 

**Output** </br>
The output should be **returned** as an **array of objects**. Each object has **two properties**: the figure's **area** and **volume**. </br>
**[ </br>
&emsp;  { area: ${area1}, volume: ${volume1} }, </br>
&emsp;  { area: ${area2}, volume: ${volume2} }, </br>
&emsp;  . . . </br>
]**


