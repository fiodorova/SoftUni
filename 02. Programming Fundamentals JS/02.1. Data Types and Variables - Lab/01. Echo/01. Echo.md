**1.	Echo Type**

Write a JS function that takes **one parameter** and **prints** on two lines the **type** of the parameter and then one of the following: </br>
&emsp; •	If the parameter type is either **string** or **number**, print its value </br>
&emsp; •	Otherwise, print the text **'Parameter is not suitable for printing'**

**Hints** </br>
&emsp; •	Write a function that receives a single **parameter**. </br>
&emsp; •	Use the console.log function to print text on the console. Each call prints a new line automatically. </br>
&emsp; •	The **typeof operator** is used to determine the data type of a given value.


